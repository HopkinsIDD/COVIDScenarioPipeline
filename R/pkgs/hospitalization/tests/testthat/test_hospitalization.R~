test_that("create_delay_frame works",{
  X <- "input"
  varname <- "output"
  p_X <- 0.1
  X_pars <- c(10,1)
  data_ <- data.frame(
    input = 1:100,
    time = lubridate::ymd('2020-01-01') + 1:100,
    uid = 1:100
  )

  expect_error({
    1+1
  }, NULL)
})
